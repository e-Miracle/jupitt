import { Box } from "@chakra-ui/react";
import { Suspense } from "react";
export default function ContentManagement() {
    return (
      <Suspense>
        <Box> ContentManagement</Box>
      </Suspense>
    );
}