import { Box } from "@chakra-ui/react";
import { Suspense } from "react";
export default function NotificationManagement() {
  return (
    <Suspense>
      <Box> NotificationManagement</Box>
    </Suspense>
  );
}
