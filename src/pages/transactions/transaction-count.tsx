import { Box } from "@chakra-ui/react";
import { Suspense } from "react";
export default function TransactionCount() {
  return (
    <Suspense>
      <Box>TransactionCount</Box>
    </Suspense>
  );
}
