import { Box } from "@chakra-ui/react";
import { Suspense } from "react";
export default function ManageRoles() {
    return (
      <Suspense>
        <Box> ManageRoles</Box>
      </Suspense>
    );
}