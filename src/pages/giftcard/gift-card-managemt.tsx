import { Box } from "@chakra-ui/react";
import { Suspense } from "react";
export default function GiftCardManagement() {
  return (
    <Suspense>
      <Box>GiftCardManagement</Box>
    </Suspense>
  );
}
