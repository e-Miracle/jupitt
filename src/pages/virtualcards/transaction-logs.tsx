import { Box } from "@chakra-ui/react";
import { Suspense } from "react";
export default function TransactionLogs() {
  return (
    <Suspense>
      <Box>TransactionLogs</Box>
    </Suspense>
  );
}
